name: ChatterBat

options:
  bundleIdPrefix: com.chatterbat
  developmentLanguage: en
  deploymentTarget:
    macOS: "14.0"

configs:
  Debug: debug
  Release: release

settings:
  base:
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    SWIFT_VERSION: "6.0"
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: YES
  configs:
    Debug:
      ENABLE_TESTABILITY: YES
      ONLY_ACTIVE_ARCH: YES
    Release:
      ENABLE_TESTABILITY: NO

targets:
  ChatterBat:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: ChatterBat
    settings:
      base:
        PRODUCT_NAME: ChatterBat
        PRODUCT_BUNDLE_IDENTIFIER: com.chatterbat.app
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_NSHumanReadableCopyright: "© 2026 ChatterBat"
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        CODE_SIGN_ENTITLEMENTS: ChatterBat/Resources/ChatterBat.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor

  ChatterBatTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: ChatterBatTests
    dependencies:
      - target: ChatterBat
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.chatterbat.tests
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/ChatterBat.app/Contents/MacOS/ChatterBat"
        BUNDLE_LOADER: "$(TEST_HOST)"

  ChatterBatUITests:
    type: bundle.ui-testing
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: ChatterBatUITests
    dependencies:
      - target: ChatterBat
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.chatterbat.uitests
        TEST_TARGET_NAME: ChatterBat

schemes:
  ChatterBat:
    build:
      targets:
        ChatterBat: all
        ChatterBatTests: [test]
        ChatterBatUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ChatterBatTests
        - ChatterBatUITests
    archive:
      config: Release
